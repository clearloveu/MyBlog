spring:
  thymeleaf:
    mode: HTML
  profiles:
    active: dev
  messages:
    basename: i18n/messages
  resources:
    static-locations: classpath:/static/,file:/home/admin/serverStatus/ServerStatus/status/json/

comment.avatar: /images/avatar.png

server:
  port: 8080
